schema_version: 1

context:
  version: "0.2.3"
  python_min: "3.12"

package:
  name: tau-ai
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/t/tau-ai/tau_ai-${{ version }}.tar.gz
    sha256: 7d210a62aa28b21d5c6f8a1c19683f2e0638044e1f6860e252c40d169a54c592

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - hatchling >=1.26
  run:
    - python >=${{ python_min }}
    - anyio >=4.0.0
    - httpx >=0.27
    - packaging >=24.0
    - pydantic >=2.0
    - rich >=13.0
    - textual >=1.0
    - typer >=0.12

tests:
  - python:
      imports: tau_agent
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  license: MIT
  license_file: LICENSE
  homepage: https://twotimespi.dev
  repository: https://github.com/huggingface/tau
  summary: A Python implementation of a minimalist Pi-style coding-agent harness.

extra:
  recipe-maintainers:
    - davidbrochart
